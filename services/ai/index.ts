// Export types
export type { AIMessage, AIStructuredResponse, AIService } from './types';

// Export the main service manager as default
import aiServiceManager from './manager';
export default aiServiceManager;
